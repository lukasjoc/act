CC being able to store remote host addresses
CC benig able to address certain known hosts
CC counter inital value should be a param in the spawn
```
actor counter inital =
  DEC {1 -=},
  INC {1 -+};
spawn {counter 37};
spawn {counter 138};
```

- create new actors
- decide what to do on certain messages
```
- default messages to set value for each actor
```act
foobar <- SET 5;
foobar <- SET 0;
```

?? custom pre-emptive scheduler (from scratch in ANSI C) (CGO)
